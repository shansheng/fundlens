# FundLens 基金穿透（Look-through）功能规划

> 版本 v1.1（三项决策已裁定，2026-09-08 01:17 用户确认）· 基于 main@2c97901 · 本文件为规划产出，未动代码
> 入口：侧边栏新增「基金穿透」，插在「策略信号」上方（`src/App.tsx` NAV 数组 `/reports` 与 `/strategy` 之间）
> 总原则：**只读分析层**——与策略信号同一原则，穿透结果绝不自动改持仓、不产生流水（v9 模型红线）

---

## 0. 一句话结论

把 N 只基金的"黑盒"拆成一张组合层面的**虚拟底层资产表**：两级行业穿透（9 大类 + 东财细分直出）+ 个股穿透（隐性重仓 / 基金重合）+ **当日行业贡献（P0，已裁定）**。数据侧最大程度复用现有资产（`disclosures` 重仓披露、`quotes_cache` 行情、东财数据通道），只新增：`stock_profile` 股票行业画像表 + 一个聚合纯函数 + Tauri 命令 + 一个页面。

---

## 1. 功能定位：穿透回答的三个问题

基金组合里持有人真正关心、但现有 UI 回答不了的：

1. **我实际持有什么？**——"我买了 5 只基金"≠分散，穿透后可能 60% 都是医药。
2. **暴露是否集中、重复？**——3 只主动基金的重仓股高度重合 = 伪分散；行业暴露向单一大类倾斜 = 回撤放大器。
3. **隐性重仓股是谁？**——单只基金 top10 里茅台只占 6%，但经 3 只基金合计，组合层面茅台可能占 8%，这是持有人感知不到的集中度。

功能定位为**持仓体检的结构化呈现**（组合画像 → 风险暴露 → 结构性问题点名），不做买卖建议——那是策略信号的职责，两层互相独立。

---

## 2. 专业口径裁定（P0 红线，实现不得走样）

| # | 裁定 | 说明 / 理由 |
|---|---|---|
| 1 | **穿透公式** | 股票 s 的穿透市值 = Σᵢ (基金 i 当前市值 × wᵢₛ)，wᵢₛ = 基金 i **最新披露期**该股占净值比例（`disclosures.weight`，0~1）。行业占比 = 行业内股票穿透市值合计 ÷ 组合总市值 |
| 2 | **不放大原则（默认）** | 披露权重直接使用，**绝不按股票仓位归一化放大**。季报 top10 通常只覆盖股票仓位的 40%~70%，放大 = 虚构精度，在披露时滞下更失真。（P2 可加「按股票仓位归一」开关，默认关，开启时页面显著标注） |
| 3 | **未穿透桶显式单列** | 现金 / 债券 / 未披露股票合并为「未穿透」桶，与行业并列展示，让用户永远看到"我知道多少、不知道多少" |
| 4 | **覆盖率定义** | 基金覆盖率 = min(1, Σw)；组合覆盖率 = Σ(基金市值 × 基金覆盖率) ÷ 总市值。口径条常驻显示 |
| 5 | **报告期口径** | 每只基金取各自**最新** report_period（`candidate_periods` 已保证），跨基金混合期如实标注（如「2026Q2×5 / 2026Q1×3」）；口径条提示披露时滞（季报约滞后 15 个工作日，中报/年报全量但滞后 2~3 个月） |
| 6 | **不穿透清单** | 货基 / 理财（002/005，份额×官方净值口径，无股票持仓）、金额兜底持仓（无真实 6 位码）→ 计入「现金理财 · 未穿透」，**绝不**用持仓收益反推股票暴露 |
| 7 | **境外股票** | 非 6 位数字代码（5 位数字 = 港股、字母 = 美股，QDII 底层常见）P0 归「境外资产」大类（穿透市值照常计入），P1 再细分行业 |
| 8 | **只读原则** | 穿透只读 `positions` + `disclosures`，不回写任何表、不产生流水；「执行」类动作一律不存在 |
| 9 | **当日行业贡献**（已裁定进 P0） | 行业当日贡献 = Σ(股票穿透市值 × 该股当日涨跌幅)。仅交易时段展示；是**基于披露权重的近似值**，未穿透部分不计入；境外股票按其所在市场的行情时点计算，与 A 股当日可能不同步（QDII T+1），页面标注。行情复用既有估算链路（`quotes_cache` + 节流），不新增出站压力 |

> 复用说明：现有盘中估算链路已经在用 `disclosures × quotes` 估净值，穿透的当日行业贡献复用同一行情缓存与节流参数。

---

## 3. 行业分类方案（穿透的"医药 / 消费 / 科技"从哪来）

### 3.1 数据源

- **候选**：东财 push2 单股详情接口的行业字段（与 `fundf10` 披露接口同族、同为公开网页接口）。⚠️ 字段名与返回格式属 P0 实测核对项——公开接口无正式契约，解析必须容错，失败兜底「未分类」。
- **缓存**：新表 `stock_profile`，命中后不再出站；批量拉取走既有 `throttle_wait()` 节流 + `disclosure_client()` 8 秒超时模式。
- 判别规则：6 位数字 → A 股；5 位数字 → 港股；其余 → 美股。

### 3.2 两级行业体系（已裁定：9 大类 L1 + 东财细分 L2）

L1 大类映射（东财行业名 → 大类，Rust 常量表，初始约 90 条）：

| 大类 | 代表性东财行业（示例） |
|---|---|
| 医药医疗 | 中药 / 西药 / 生物制品 / 医疗器械 / 医疗服务 / 医药商业 |
| 主要消费 | 酿酒 / 食品饮料 / 农牧饲渔 |
| 可选消费 | 汽车整车 / 汽车零部件 / 家电 / 旅游酒店 / 商贸零售 / 纺织服装 |
| 科技 TMT | 半导体 / 电子元件 / 光学光电子 / 通信设备 / 软件开发 / 互联网服务 / 计算机设备 / 游戏 / 传媒 |
| 金融地产 | 银行 / 保险 / 证券 / 多元金融 / 房地产 |
| 高端制造 | 工程机械 / 电力设备 / 光伏设备 / 风电设备 / 电池 / 仪器仪表 |
| 周期资源 | 煤炭 / 钢铁 / 有色金属 / 石油 / 化学原料 / 化肥 |
| 基建公用·交运 | 电力 / 公用事业 / 航运港口 / 铁路公路 / 物流 / 工程建设 |
| 国防军工 | 航天航空 / 军工 |
| **未穿透** | 现金 / 债券 / 未披露股票（虚拟桶，非映射产物） |
| **境外资产** | 港股 / 美股（P0 整桶，P1 细分） |
| **未分类** | 行业拉取失败或映射未命中的兜底桶，进「待补行业」清单可重试 |

**L2 细分行业 = 东财行业名直出**（`stock_profile.industry_em` 原样作为第二级，如 酿酒 / 半导体 / 光伏设备 / 通信设备），**零额外维护成本**；UI 提供「大类 / 细分」两级切换，两级权重分母一致（同为组合总市值）。

映射未命中 → 归「未分类」并记日志，**不阻塞主流程**；映射表可后续持续补充。

---

## 4. 数据与命令层设计

### 4.1 新表（`src-tauri/src/db.rs`，遵守 init_db 红线）

```sql
CREATE TABLE IF NOT EXISTS stock_profile (
  stock_code TEXT PRIMARY KEY,
  name TEXT,
  industry_em TEXT,                    -- 东财行业名（原始）= L2 细分
  sector_l1 TEXT NOT NULL DEFAULT '未分类',  -- 九大类映射结果 = L1
  market TEXT,                         -- 'A' | 'HK' | 'US'
  source TEXT,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

`IF NOT EXISTS` 幂等，后续演进走 `ensure_column`，严禁 DROP。

### 4.2 命令（三步缺一不可：commands.rs → lib.rs `generate_handler!` → ACL 双处）

| 命令 | 作用 | ACL |
|---|---|---|
| `lookthrough_overview` | 组合穿透主查询：两级行业数组（L1+L2）+ 个股数组（含当日涨跌）+ 基金明细（各自报告期/覆盖率）+ 未穿透桶 + 汇总口径；交易时段 join `quotes_cache` 算当日贡献 | `fl-lookthrough-overview` |
| `fetch_stock_profiles` | 批量补行业画像（只拉缺失/过期，节流） | `fl-fetch-stock-profiles` |
| `lookthrough_fund(code)`（P1） | 单基金穿透 | `fl-lookthrough-fund` |
| `lookthrough_overlap`（P1） | 基金两两重合矩阵 | `fl-lookthrough-overlap` |

### 4.3 聚合引擎（新 `src-tauri/src/lookthrough.rs`）

纯函数 `aggregate(positions, latest_disclosures_per_fund, profiles, quotes?) -> LookthroughResult`，与 IO 解耦，可直接单测。

**写进测试的不变量**：

1. Σ(全部分类权重) = 100% ± 1e-6（L1 行业 + 未穿透 + 境外 + 未分类）；
2. 单只股票穿透市值 ≤ 持有它的基金市值之和；
3. 同一基金多期披露只取最新期（防止跨期合并成"曾持有"）；
4. 货基/理财/金额兜底不产生任何个股行；
5. L2 细分权重合计 = 其所属 L1 大类权重（两级自洽）。

---

## 5. UI / 交互设计（`src/pages/LookthroughPage.tsx`，路由 `/lookthrough`）

页面自上而下：

1. **口径条（常驻，warning 语气）**：组合覆盖率 · 报告期分布 · 时滞提示；右侧「抓取披露持仓」「补行业画像」两个入口（复用总览现有按钮交互模式）。
2. **Tab 1 行业穿透（P0）**：横向条形图——**「大类 / 细分」两级切换（已裁定）**；占比条长 = 穿透市值，右列**当日贡献（P0，红涨绿跌，仅交易时段）**；「未穿透」桶虚线样式区隔；点击行业钻取该行业下个股列表（P1）。
3. **Tab 2 个股穿透（P0）**：虚拟重仓表——股票 / 大类 / 穿透权重 / 穿透市值 / 持有基金数 / 当日涨跌（P0），行可展开看贡献基金明细；顶部 CR5/CR10 集中度徽标；**隐性重仓预警**（已裁定阈值：同一股票经 ≥3 只基金持有且合计穿透占比 >5% 时 warning 徽标）。
4. **Tab 3 基金重合（P1）**：两两基金重仓股重合度（权重重合 Σmin(wᵢ,wⱼ) + top10 Jaccard 双口径），识别"伪分散"。

工程约束：

- 窄屏（<lg）双路径：口径条折叠为一行徽标、条形图全宽、表格转卡片——参照总览 hero 条模式；
- 颜色全走 CSS 令牌（`readColorVar` 渲染期解析），红涨绿跌仅用于当日贡献/涨跌列，行业条形用单一中性信息色 ramp，**源码禁止硬编码色值**（P0 红线）；
- 刷新遵循既有节奏：交易时段复用总览 15 分钟定时器思路，非交易时段不发起行情请求（麒麟刷新卡死防御同款超时参数）；
- 空态：无 disclosures → 引导「抓取披露持仓」；行业缺失 → 「补行业画像」；均有明确下一步动作。

---

## 6. 分期实施

| 期 | 内容 | 说明 |
|---|---|---|
| **P0**（首个可用版） | `stock_profile` 表 + 两级行业体系（9 大类映射 + 东财细分直出）+ `fetch_stock_profiles` + `lookthrough_overview`（**含当日行业贡献**：交易时段 join quotes_cache）+ `LookthroughPage`（行业穿透【大类/细分切换 + 当日贡献】+ 个股穿透【含当日涨跌列】）+ 口径条/空态 + 聚合不变量单测 | 当日贡献复用既有估算行情链路，仍可一个 session 交付 |
| **P1** | 行业钻取、隐性重仓预警交互化、基金重合矩阵、FundDetailPage 单基金穿透卡片、境外股票行业细分；**核对 jjcc 接口中报/年报能否取全量持仓**（现有 `topline=10` 参数是否限制全量需实测，若受限则补参数） | 重合度与钻取把分析做深 |
| **P2** | 穿透口径开关（按股票仓位归一）、风格箱（大小盘 × 价值成长估算）、行业拥挤度与策略信号联动提示、用户自定义主题标签 | 锦上添花 |

---

## 7. 工程落地清单（文件级）

| 文件 | 改动 |
|---|---|
| `src-tauri/src/db.rs` | `stock_profile` 建表 + 读写函数 |
| `src-tauri/src/lookthrough.rs`（新） | 聚合纯函数 + 两级行业映射常量 |
| `src-tauri/src/data.rs` | `fetch_stock_industry`（东财，节流复用） |
| `src-tauri/src/commands.rs` | 上述命令 |
| `src-tauri/src/lib.rs` | `mod lookthrough` + `generate_handler!` 注册 |
| `src-tauri/permissions/fundlens.toml` + `capabilities/default.json` | ACL 双处 |
| `src/api.ts` | `lookthroughOverview` / `fetchStockProfiles` + 类型 |
| `src/pages/LookthroughPage.tsx`（新） | 页面 |
| `src/App.tsx` | NAV 插入（策略信号上方）+ Route |
| 麒麟分支 | 收尾同步：`api.ts`（v1 invoke 差异）/ `lib.rs`（无 dialog plugin）/ `db.rs`（path_resolver Option）三文件按既有适配要点合并，其余前端整覆盖；fl-build 容器重打包 |

---

## 8. 测试与验收

- `npx tsc -b` 0 错误；`npx vitest run` 全过（39 + 新增）；
- `cargo test --lib --no-default-features`（99 + 新增，聚合不变量必测）；
- **复现式验证**（项目惯例）：用真实 DB（`~/Library/Application Support/com.fundlens.app/fundlens.db`）任选一只基金，手算其 top10 行业占比与页面输出比对；
- **当日贡献交叉校验**：交易时段 Σ个股当日贡献 应与既有估算链路的组合当日估算同源可对账（同 quotes、同权重，两者差异仅来自估算链路的个股权重口径）；
- 手工核对：未穿透桶 + 全行业和 = 100%；货基不出现在个股表；混合报告期标注正确；L2 合计 = L1。

---

## 9. 风险与边界

- **接口风险**：东财公开网页接口无契约，行业字段可能变化 → 解析容错 + 失败兜底「未分类」，绝不阻塞主流程；
- **披露时滞**：穿透是**近似画像**而非实时持仓，口径条常驻提示，这是诚实性问题不是 bug；
- **当日贡献近似性**：基于披露权重的近似，未穿透部分不计；QDII 底层境外股票行情时点与 A 股不同步，页面标注；
- **映射维护**：初始约 90 条覆盖主流行业，未命中进待补清单，成本可控；
- 本功能为持仓结构分析工具，输出不构成投资建议。

> **免责声明**：以上内容基于公开数据和量化分析，仅供参考，不构成投资建议。市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况和投资目标独立判断，必要时咨询持牌专业机构。过往表现不预示未来收益。

---

## 10. 决策裁定（2026-09-08 01:17 用户确认，勿回退）

1. **行业体系**：采用 **9 大类**，并在其上加**细分二级**（东财行业名直出，零维护成本）；P0 即支持大类/细分两级切换。P2 再考虑用户自定义主题标签。
2. **隐性重仓预警阈值**：**已裁定**——同一股票经 **≥3 只基金**持有且**合计穿透占比 >5%** 触发预警。
3. **当日行业贡献**：**已裁定进 P0**——交易时段展示，复用既有估算行情链路（quotes_cache + 节流）；未穿透部分不计入，境外股票标注行情时点差异。
