# FundLens v2.5.1 发版记录 — Hotfix：指数/个股行情同码碰撞（001551 两页估值不一致）

日期：2026-09-09 ｜ 分支：main `121d771`+`737fbfd` / 麒麟待同步 ｜ Android：不打 APK

## 1. 版本与交付物

| 项 | 值 |
|---|---|
| 版本 | 2.5.0 → **2.5.1**（五处同步） |
| 修复 | ✅ 指数行情与个股行情分离批抓取（commands.rs get_overview） |
| 桌面 | ✅ /Applications/FundLens.app v2.5.1 已部署（PlistBuddy=2.5.1、部署 mtime 22:47:20 ≥ build 22:22:45） |
| 麒麟 | 待同步 |
| 提交 | main `121d771`（fix）+`737fbfd`（升位）+release 文档；tag `v2.5.1` |

## 2. Bug：001551 持仓列表「估算收益率」+5.20% vs 详情页「当日估算收益」-1.41%

### 现象
同一基金同一时刻两页头条估值差 6.61pp：列表 +5.2038%、详情 -1.4091%，且都自报 valuation_method="index"。

### 根因（真实库端到端定位）
- `fetch_quotes` 行情 HashMap 按**去交易所前缀的数字码**为 key（data.rs:730-734）。
- `get_overview` 原把**全部基准指数 + 全部重仓股**合并进同一批行情请求（839 符号）。
- 001551 跟踪的**中证医药指数 `sh000933`** 与组合内另一基金披露的**神火股份 `sz000933`** 共享末 6 位 `000933` → 同批内个股覆盖指数 → `index_quotes["000933"]`=神火股份（当日 +5.2038%）→ 001551 指数代理被错配成个股行情。
- `get_fund_detail` 单只独立拉取（commands.rs:674-676）无碰撞 → 正确 −1.4091%。
- 影响面：凡组合同时持有「sh000xxx 中证系指数基金」+「同数字码 sz000xxx 个股」（如 000905↔厦门港务、000688↔国城矿业）均会触发，系统性批量估值坑。

### 修复（commit 121d771）
- `get_overview`：指数基准与个股**分两批独立拉取**——个股批（digit key，供披露穿透查找，A 股跨交易所同码无交集故安全）+ 指数批（仅指数符号，digit 键唯一）；`index_quotes` 改从指数批取值。行为与 `get_fund_detail` 对齐，对外 API/字段零变化。
- 验证：真实库隔离对拍，修复后 overview 与 detail 对 001551 的 est_change_pct / day_pnl_pct_est **均 = −1.4091%**（中证医药正确值）；`cargo test --lib --no-default-features` 141 passed 无回归。

## 3. 测试指引（App 内人工验收）
1. 打开持仓列表与 001551 详情页：两者估值应一致（今日中证医药 -1.409% 口径）。
2. 抽查其它指数基金（如跟踪 000905/000300 的联接/ETF 持仓），列表估值应与详情页一致、且贴合跟踪指数当日涨跌。
3. 桌面 v2.5.1 已部署核验见文末（未签名 ad-hoc，首次打开右键→打开）。
